<div class="container text-left">
  <div class="justify-content-center text-left ml-4">
       <div class="form">
        <p>Author name:</p>
  <h1><%= @authors.name %></h1>
  <p>Auhtors books: </p>
 <% @author.book.each do |book| %>
 <h2><%= book.title %></h2>
  <h3><%= book.genre %></h3>
  <%# if @books.photo.attached? %>
      <%#= cl_image_tag @authors.book.photo.key, height: 300, width: 400, crop: :fill %>
    <%# end %>
 <% end %>

      <div class="justify-content-center text-left ml-4">
        <div class="form">
          <%= link_to authors_path, method: :delete, data: { confirm: "Are you sure?" } do %>
          <ul class="text-success"><i class="fa fa-trash"></i></ul>
          <% end %>
          </div>
            <div class="button mb-4 mt-4">
              <%= link_to "Add book", author_books_path(params[:id]) %>
            </div>
            <%#= link_to "check book", author_book_path(params[:id])  %>
    </div>
  </div>
    <div class="button ml-4">
      <%= link_to 'Back', authors_path %>
    </div>
</div>
